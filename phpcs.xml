<?xml version="1.0"?>
<ruleset name="ZentiqLabs FastPdf">
    <description>Coding standard for zentiq-labs/fast-pdf</description>

    <file>src</file>
    <file>tests</file>

    <arg name="basepath" value="."/>
    <arg name="colors"/>
    <arg value="sp"/>
    <arg name="extensions" value="php"/>

    <rule ref="PSR12"/>

    <rule ref="Generic.Files.LineLength">
        <properties>
            <property name="lineLimit" value="120"/>
            <property name="absoluteLineLimit" value="0"/>
        </properties>
    </rule>

    <rule ref="Squiz.WhiteSpace.SuperfluousWhitespace"/>
    <rule ref="Generic.PHP.RequireStrictTypes"/>

    <!-- Pest test files declare helper functions alongside test closures,
         which PSR-1 considers a side effect. Suppress for the entire tests/
         directory since this is an intentional Pest pattern, not a violation. -->
    <rule ref="PSR1.Files.SideEffects.FoundWithSymbols">
        <exclude-pattern>tests/*</exclude-pattern>
    </rule>
</ruleset>
